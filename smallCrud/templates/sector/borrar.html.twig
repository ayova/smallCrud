{% extends "base.html.twig" %}

{% block body %}
<div class="container col-5 mx-auto text-center">
    <p class="mt-3 mx-auto">Estas seguro de querer borrar el siguiente sector?
        <h2>{{ sector.nombreSector }}</h2>
    </p>
    {# botones #}
    <a class="btn btn-danger" href="{{ path('sector_borrar', {'id': sector.id }) }}">Borrar</a>
    <a class="btn btn-primary" href="{{ path('sector_index') }}">Cancelar</a>
</div>
{% endblock %}

